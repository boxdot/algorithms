env:
  - CXX=clang++-3.6
addons:
  apt:
    sources:
      - llvm-toolchain-precise-3.6
      - ubuntu-toolchain-r-test
    packages:
      - clang-3.6
      - g++-4.9  # brings libstd for clang
script:
  - make
  - make -j
  - make test
